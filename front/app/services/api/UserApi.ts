// import { api } from "./RestApi";

// const USER_ROUTE = "/auth/";

// export async function Login(body: User): Promise<string> {
//   const response = await api.create<User>(USER_ROUTE + "login", body);
//   return response.data ?? ;
// }

// export async function Register(body: User): Promise<string> {
//   const response = await api.create<User>(USER_ROUTE + "register", body);
//   return response.data ?? "";
// }
